[package]
name = "shelfarrs-sdk"
version = "0.0.0"
edition = "2021"
description = "Shared plugin ABI types for shelfarr-rs (host + plugins depend on this)"

[dependencies]
serde = { version = "1", features = ["derive"] }
